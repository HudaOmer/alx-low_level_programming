#include <stdio.h>

/**
 * main - main function
 *
 * Description: prints numbers
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf(i);
	putchar('\n');

	return (0);
}
