#include <stdio.h>

/**
 * main - main function
 *
 * Description: prints alphabets
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	char ch = 'a'

	for (ch = 'a';ch <= 'z'; ch++)
		putchar(ch);
	putchar(' ');
}
